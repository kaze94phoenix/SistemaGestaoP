@model IEnumerable<SistemaGestaoP.Models.Disciplina_Professor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Disciplina.designacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Professor.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Disciplina.designacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Professor.nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DisciplinaProfessor_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.DisciplinaProfessor_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DisciplinaProfessor_id })
        </td>
    </tr>
}

</table>
