@model IEnumerable<SistemaGestaoP.Models.Seccao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.designacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.designacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Seccao_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Seccao_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Seccao_id })
        </td>
    </tr>
}

</table>
